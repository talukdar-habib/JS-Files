const computer = {
    brand : 'Dell',
    price : 40000,
    processor : 'intel',
    hdd : '1tb',
    monitor : 'samsung'
}

const keys = Object.keys(computer); //property/keys gula eksathe access kora jay 
console.log(keys);
